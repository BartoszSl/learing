const div = document.querySelector('div')
const p = document.querySelector('p')

// removeChild

div.removeChild(p)

// remove 

p.remove() // nie musimy odnosić się do rodzica, tylko możemy od razu usnać interesujacy nas element